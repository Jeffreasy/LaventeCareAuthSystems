# ================================
# CORE CONFIGURATION
# ================================

# Application Environment
# Options: development, staging, production
APP_ENV=production

# Server Configuration
# Port the API listens on (Render auto-injects this, but 8080 is default)
PORT=8080

# Application Base URL
# This is used for email links, redirects, etc.
# Example: https://auth.laventecare.nl or https://laventecare-api.onrender.com
APP_URL=https://your-app-url.onrender.com

# ================================
# DATABASE
# ================================

# PostgreSQL Connection String
# Render auto-injects this from the linked database service
# Format: postgres://user:password@host:port/database?sslmode=require
DATABASE_URL=${DATABASE_URL}

# ================================
# SECURITY & AUTHENTICATION
# ================================

# JWT Secret Key
# CRITICAL: Must be at least 32 characters, randomly generated
# Generate with: openssl rand -base64 32
# Or use the provided ./scripts/generate-secrets.sh script
JWT_SECRET=CHANGE_ME_MINIMUM_32_CHARACTERS_RANDOM_STRING

# Public Registration
# Set to 'true' to allow anyone to register
# Set to 'false' to require admin invitations only
ALLOW_PUBLIC_REGISTRATION=false

# ================================
# OBSERVABILITY (OPTIONAL)
# ================================

# Sentry DSN for Error Tracking
# Sign up at https://sentry.io and create a project
# Leave empty to disable Sentry
SENTRY_DSN=

# ================================
# RENDER-SPECIFIC NOTES
# ================================

# 1. DATABASE_URL: Auto-injected by Render from linked PostgreSQL service
# 2. JWT_SECRET: Set in Render Dashboard → Environment → Add Environment Variable
#    - Use 'Generate Value' or paste your own secure random string
# 3. SENTRY_DSN: Set in Render Dashboard if using Sentry
# 4. APP_URL: Update to your actual Render URL (e.g., https://laventecare-api.onrender.com)

# ================================
# LOCAL DEVELOPMENT OVERRIDES
# ================================

# For local development, create a .env.local file with:
# DATABASE_URL=postgres://user:password@localhost:5488/laventecare?sslmode=disable
# APP_ENV=development
# JWT_SECRET=dev-secret-key-not-for-production
